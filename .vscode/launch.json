{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "launch with gcc",
      "type": "cppdbg",
      "request": "launch",
      "cwd": ".",
      "program": "${workspaceRoot}/a.out",
      "preLaunchTask": "build with gcc"
    }
  ]
}
