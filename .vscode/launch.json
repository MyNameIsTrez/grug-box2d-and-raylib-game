{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Rebuild and run",
      "type": "cppdbg",
      "request": "launch",
      "cwd": "${workspaceRoot}",
      "program": "${workspaceRoot}/build/game",
      "preLaunchTask": "Rebuild"
    }
  ]
}
